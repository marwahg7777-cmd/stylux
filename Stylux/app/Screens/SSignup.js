import { View, Text } from 'react-native'
import React from 'react'

const SSignup = (navigation) => {
  return (
    <View>
      <Text style={{fontWeight: "bold", fontSize: 50, textAlign: 'center', marginTop: 40, color: "orange"}}>Malta</Text>
    </View>
  )
}

export default SSignup